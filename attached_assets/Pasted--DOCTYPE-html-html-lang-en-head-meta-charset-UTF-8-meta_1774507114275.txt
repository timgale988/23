<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Results — Actionable Insight AI</title>
<link href="https://fonts.googleapis.com/css2?family=Urbanist:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
body {
  font-family: 'Urbanist', sans-serif;
  background: #fff;
  color: #0A0E1A;
  line-height: 1.6;
}

:root {
  --dark: #0A0E1A;
  --white: #ffffff;
  --cream: #F7F8FC;
  --border: #E8ECF2;
  --muted: #6B7A90;
  --muted-light: #9AA3B2;
  --violet: #7C3AED;
  --blue: #2563EB;
  --cyan: #06B6D4;
  --grad: linear-gradient(90deg, #7C3AED, #2563EB, #06B6D4);
}

/* ── Results hero ── */
.results-hero {
  background: var(--dark);
  padding: 96px 60px 80px;
  position: relative;
  overflow: hidden;
}
.hero-grid-bg {
  position: absolute; inset: 0; opacity: .04;
  background-image:
    linear-gradient(rgba(255,255,255,.8) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,.8) 1px, transparent 1px);
  background-size: 64px 64px;
}
.hero-accent {
  position: absolute; bottom: -80px; right: 10%;
  width: 400px; height: 400px;
  background: linear-gradient(135deg, #7C3AED, #2563EB, #06B6D4);
  opacity: .1; border-radius: 50%; filter: blur(80px);
}
.hero-inner { position: relative; z-index: 2; }
.hero-eyebrow {
  display: flex; align-items: center; gap: 12px;
  font-size: 11px; letter-spacing: .22em; text-transform: uppercase;
  font-weight: 700; color: rgba(255,255,255,.35); margin-bottom: 28px;
}
.hero-eyebrow::before {
  content: ''; display: block; width: 32px; height: 1px;
  background: rgba(255,255,255,.2);
}
.hero-h1 {
  font-size: 72px; font-weight: 700; letter-spacing: -.04em;
  text-transform: uppercase; color: #fff; line-height: .9; margin-bottom: 28px;
  max-width: 700px;
}
.grad-text {
  background: var(--grad);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.hero-sub {
  font-size: 17px; color: rgba(255,255,255,.5); line-height: 1.7;
  font-weight: 300; max-width: 540px;
}

/* ── Summary metrics bar ── */
.metrics-bar {
  display: grid; grid-template-columns: repeat(4, 1fr);
  border-bottom: 1px solid var(--border);
  background: var(--cream);
}
.metric {
  padding: 44px 48px;
  border-right: 1px solid var(--border);
}
.metric:last-child { border-right: none; }
.metric-num {
  font-size: 52px; font-weight: 700; letter-spacing: -.05em;
  line-height: 1; color: var(--dark); margin-bottom: 8px;
}
.metric-num em {
  font-style: normal;
  background: var(--grad);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.metric-label {
  font-size: 11px; color: var(--muted-light); text-transform: uppercase;
  letter-spacing: .09em; line-height: 1.5; font-weight: 600;
}

/* ── Case study section ── */
.case-section {
  padding: 96px 60px;
  border-bottom: 1px solid var(--border);
}
.section-eyebrow {
  display: flex; align-items: center; gap: 12px;
  font-size: 10px; letter-spacing: .22em; text-transform: uppercase;
  font-weight: 700; color: var(--violet); margin-bottom: 24px;
}
.section-eyebrow::before {
  content: ''; display: block; width: 24px; height: 1px;
  background: var(--violet);
}
.section-h2 {
  font-size: 48px; font-weight: 700; letter-spacing: -.04em;
  text-transform: uppercase; color: var(--dark); line-height: .92;
  margin-bottom: 16px;
}
.section-lead {
  font-size: 16px; color: var(--muted); line-height: 1.7;
  font-weight: 300; max-width: 560px; margin-bottom: 56px;
}

/* ── Single case study layout ── */
.case-layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1px;
  background: var(--border);
  border: 1px solid var(--border);
}
.case-main {
  background: #fff;
  padding: 52px 52px;
  position: relative;
}
.case-main::before {
  content: ''; position: absolute;
  top: 0; left: 0; width: 100%; height: 3px;
  background: var(--grad);
}
.case-sidebar {
  background: var(--cream);
  padding: 52px 48px;
  display: flex;
  flex-direction: column;
  gap: 0;
}
.case-client {
  font-size: 10px; letter-spacing: .22em; text-transform: uppercase;
  font-weight: 700; color: var(--muted-light); margin-bottom: 20px;
}
.case-headline {
  font-size: 36px; font-weight: 700; letter-spacing: -.03em;
  text-transform: uppercase; color: var(--dark); line-height: .95;
  margin-bottom: 20px;
}
.case-headline em {
  font-style: normal;
  background: var(--grad);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.case-body {
  font-size: 15px; color: var(--muted); line-height: 1.75;
  font-weight: 300; margin-bottom: 40px;
}
.case-stats {
  display: grid; grid-template-columns: repeat(3, 1fr);
  gap: 0; border-top: 1px solid var(--border); padding-top: 32px;
}
.case-stat { padding-right: 24px; }
.case-stat:last-child { padding-right: 0; }
.case-stat-num {
  font-size: 32px; font-weight: 700; letter-spacing: -.03em;
  color: var(--dark); line-height: 1; margin-bottom: 6px;
}
.case-stat-label {
  font-size: 10px; color: var(--muted-light); text-transform: uppercase;
  letter-spacing: .08em; font-weight: 600; line-height: 1.4;
}

/* Sidebar data points */
.sidebar-label {
  font-size: 10px; letter-spacing: .18em; text-transform: uppercase;
  font-weight: 700; color: var(--muted-light); margin-bottom: 20px;
}
.sidebar-stats {
  display: flex; flex-direction: column; gap: 0;
  border: 1px solid var(--border);
  background: #fff;
  margin-bottom: 28px;
}
.sidebar-row {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center;
  justify-content: space-between; gap: 16px;
}
.sidebar-row:last-child { border-bottom: none; }
.sidebar-key {
  font-size: 12px; color: var(--muted); font-weight: 400;
}
.sidebar-val {
  font-size: 14px; font-weight: 700; color: var(--dark);
  letter-spacing: -.01em; text-align: right;
}
.sidebar-val.accent {
  background: var(--grad);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}

.sidebar-note {
  font-size: 13px; color: var(--muted); line-height: 1.65;
  font-weight: 300; padding: 20px; background: #fff;
  border: 1px solid var(--border);
  border-left: 3px solid var(--violet);
}

/* ── Pipeline breakdown ── */
.pipeline-section {
  padding: 96px 60px;
  background: var(--dark);
  border-bottom: 1px solid rgba(255,255,255,.06);
}
.pipeline-grid {
  display: grid; grid-template-columns: repeat(3, 1fr);
  gap: 2px; background: rgba(255,255,255,.06);
  border: 1px solid rgba(255,255,255,.06);
  margin-top: 56px;
}
.pipeline-card {
  background: #141828;
  padding: 40px 36px;
}
.pipeline-card-num {
  font-size: 44px; font-weight: 700; letter-spacing: -.04em;
  line-height: 1; margin-bottom: 10px;
  background: var(--grad);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  background-clip: text;
}
.pipeline-card-label {
  font-size: 12px; font-weight: 700; letter-spacing: .08em;
  text-transform: uppercase; color: #fff; margin-bottom: 8px;
}
.pipeline-card-desc {
  font-size: 13px; color: rgba(255,255,255,.4);
  line-height: 1.65; font-weight: 300;
}

/* ── Clients strip ── */
.clients-section {
  padding: 56px 60px;
  border-bottom: 1px solid var(--border);
  background: var(--cream);
}
.clients-label {
  font-size: 10px; letter-spacing: .22em; text-transform: uppercase;
  font-weight: 700; color: var(--muted-light); margin-bottom: 28px;
  text-align: center;
}
.clients-row {
  display: flex; justify-content: center; flex-wrap: wrap;
  border-top: 1px solid var(--border); border-left: 1px solid var(--border);
}
.client-badge {
  padding: 20px 48px;
  border-right: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
  font-size: 12px; font-weight: 700; letter-spacing: .14em;
  text-transform: uppercase; color: var(--muted);
}

/* ── Grad strip ── */
.grad-strip { height: 3px; background: var(--grad); }
</style>
</head>
<body>

<!-- HERO -->
<div class="results-hero">
  <div class="hero-grid-bg"></div>
  <div class="hero-accent"></div>
  <div class="hero-inner">
    <div class="hero-eyebrow">Client results</div>
    <h1 class="hero-h1">Proof is in<br>the <span class="grad-text">numbers.</span></h1>
    <p class="hero-sub">Verified results from Providence Health South Division — drawn from 3,098 prospects, $46.1 billion in identified wealth, and direct platform attribution.</p>
  </div>
</div>

<!-- METRICS BAR -->
<div class="metrics-bar">
  <div class="metric">
    <div class="metric-num">305<em>%</em></div>
    <div class="metric-label">Increase in first-time<br>major gifts</div>
  </div>
  <div class="metric">
    <div class="metric-num">448<em>%</em></div>
    <div class="metric-label">Increase in prospect<br>engagement</div>
  </div>
  <div class="metric">
    <div class="metric-num">$4.32<em>M</em></div>
    <div class="metric-label">Realized revenue,<br>initial deployment</div>
  </div>
  <div class="metric">
    <div class="metric-num">5.2<em>×</em></div>
    <div class="metric-label">Best vs. average<br>conversion rate</div>
  </div>
</div>

<!-- CASE STUDY -->
<div class="case-section">
  <div class="section-eyebrow">Case study</div>
  <h2 class="section-h2">Providence Health<br>South Division.</h2>
  <p class="section-lead">A multi-hospital Prysm deployment across nine California ministries — generating 461 qualified major giving prospects from 3,098 identified patients and $4.32M in realized philanthropic revenue.</p>

  <div class="case-layout">

    <!-- LEFT: narrative -->
    <div class="case-main">
      <div class="case-client">Providence Health — South Division, California</div>
      <div class="case-headline"><em>461</em> qualified<br>major giving<br>prospects.</div>
      <p class="case-body">Providence South Division deployed Prysm across nine California hospitals to build a systematic grateful patient identification program from Epic EMR data. The platform identified 3,098 prospects representing $46.1 billion in total net worth, qualifying 461 to major giving status — a 14.9% system-wide conversion rate with significant variance across ministries.<br><br>St. Joseph Burbank emerged as the highest-converting ministry at 67.5% — nearly 5× the system average — driven by inpatient visit utilization four times higher than the system norm and direct physician referral maturation. The initial deployment generated $4.32M in realized revenue, with a 305% increase in first-time major gifts and a 448% increase in engagement activity.</p>
      <div class="case-stats">
        <div class="case-stat">
          <div class="case-stat-num">461</div>
          <div class="case-stat-label">Qualified MG<br>prospects</div>
        </div>
        <div class="case-stat">
          <div class="case-stat-num">$6.6B</div>
          <div class="case-stat-label">Qualified MG<br>net worth</div>
        </div>
        <div class="case-stat">
          <div class="case-stat-num">9</div>
          <div class="case-stat-label">Hospitals<br>managed</div>
        </div>
      </div>
    </div>

    <!-- RIGHT: data sidebar -->
    <div class="case-sidebar">
      <div class="sidebar-label">Platform data — Feb 2026</div>

      <div class="sidebar-stats">
        <div class="sidebar-row">
          <div class="sidebar-key">Total prospects identified</div>
          <div class="sidebar-val accent">3,098</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Total pipeline net worth</div>
          <div class="sidebar-val accent">$46.1B</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Qualified MG prospects</div>
          <div class="sidebar-val">461</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Qualified MG net worth</div>
          <div class="sidebar-val">$6.6B</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">System conversion rate</div>
          <div class="sidebar-val">14.9%</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Best ministry conversion (SJB)</div>
          <div class="sidebar-val accent">67.5%</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Realized revenue</div>
          <div class="sidebar-val accent">$4.32M</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">First-time MG gift increase</div>
          <div class="sidebar-val">305%</div>
        </div>
        <div class="sidebar-row">
          <div class="sidebar-key">Engagement increase</div>
          <div class="sidebar-val">448%</div>
        </div>
      </div>

      <div class="sidebar-note">
        St. Joseph Burbank converts at 67.5% — nearly 5× the 14.9% system average — from inpatient visit utilization 4.5× higher than system norm and mature direct physician referral relationships.
      </div>
    </div>

  </div>
</div>

<!-- PIPELINE BREAKDOWN -->
<div class="pipeline-section">
  <div class="section-eyebrow" style="color:rgba(255,255,255,.3);">
    <span style="display:block;width:24px;height:1px;background:rgba(255,255,255,.2);"></span>
    Pipeline intelligence
  </div>
  <h2 class="section-h2" style="color:#fff;margin-bottom:16px;">$46.1 billion in<br>identified wealth.</h2>
  <p class="section-lead" style="color:rgba(255,255,255,.4);">Prysm surfaces the full scale of philanthropic opportunity from clinical data — the majority of which remains unworked and available for activation.</p>

  <div class="pipeline-grid">
    <div class="pipeline-card">
      <div class="pipeline-card-num">$46.1B</div>
      <div class="pipeline-card-label">Total identified wealth</div>
      <div class="pipeline-card-desc">Net worth of all 3,098 Prysm-identified prospects across nine South Division hospitals.</div>
    </div>
    <div class="pipeline-card">
      <div class="pipeline-card-num">$29.0B</div>
      <div class="pipeline-card-label">Unworked pipeline</div>
      <div class="pipeline-card-desc">1,621 identified prospects never contacted by a development officer — representing 63% of all pipeline wealth at $19.8M average net worth.</div>
    </div>
    <div class="pipeline-card">
      <div class="pipeline-card-num">$4.07B</div>
      <div class="pipeline-card-label">Incremental opportunity</div>
      <div class="pipeline-card-desc">224 additional MG qualifications achievable from existing pipeline using proven Burbank benchmarks — without identifying a single new prospect.</div>
    </div>
  </div>
</div>

<div class="grad-strip"></div>

<!-- CLIENTS -->
<div class="clients-section">
  <div class="clients-label">Current clients</div>
  <div class="clients-row">
    <div class="client-badge">Providence Health</div>
    <div class="client-badge">City of Hope</div>
    <div class="client-badge">LSU Health</div>
    <div class="client-badge">UCF Health</div>
  </div>
</div>

</body>
</html>